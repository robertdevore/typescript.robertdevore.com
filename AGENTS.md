# TypeScript Course

Work only in this standalone repository. Production is https://typescript.robertdevore.com.

Preserve the boundary between JavaScript runtime behavior, TypeScript analysis, and tooling assumptions. Never present annotations, assertions, declarations, or brands as runtime validation or authorization.

Run npm run verify and npm run check:package for course changes. Run npm run test:browser with the preview server for UI changes. Recheck technical currency before changing the public verification date. Use actual compiler diagnostics, not invented text. Pin compiler changes and review consumer declarations. Keep invalid fixtures out of normal builds.

Self-host Departure Mono for headings/decorative text and Inter for body text. Keep browser JavaScript below 10 KB uncompressed and load search data only on the search page. Maintain no-JS navigation, keyboard access, production canonicals, and real 404 responses.

Cloudflare scope is only the typescript-course Worker and typescript.robertdevore.com. Never edit unrelated applications or DNS. Commit meaningful changes and push the intended branch. Record durable session handoffs through the user's Strata workflow; avoid routine SignalBox captures.
